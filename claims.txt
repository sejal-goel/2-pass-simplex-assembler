Claims File
Name: Sejal Goel
Roll No: 2401CS17

ASSEMBLER (asm.cpp)
- Name and declaration of authorship at the top of the file.
- Written in C++, compiles cleanly with g++.

- Uses the one-read internal form approach: 
- pass1():
  parses the entire file into a vector of Line structs (pc, label, mnemonic, operand, line number)
- pass2():
  iterates over those to emit machine code. No redundant file IO.

- All 19 instructions plus SET and data defined in a single map-based opcode table (optable) with hex opcode and operand-type flag per entry.
- Errors diagnosed: duplicate label, invalid label name, unknown mnemonic,missing operand, unexpected operand, extra text on line, undefined label,invalid number literal.
- Warning issued for unused labels.
- Produces .o (binary, little-endian), .lst (advanced format: PC + machine code + mnemonic), and .log (errors and warnings) files.
- SET pseudo-instruction implemented and tested.
- Decimal, hex (0x) and octal (leading 0) number formats supported.
- Branch displacement computed as label_address - (current_PC + 1).
- Non-branch label operands use the absolute address.
- test1.asm assembles with no errors, unused label warning issued correctly.
- test2.asm: all errors diagnosed correctly.
- test3.asm: SET assembles correctly.
- Provided triangle/main test program assembles and runs correctly.

Bubble sort:
- bubble_sort.asm assembles and produces correct sorted output {1,3,4,5,8} in mem[200..204] verified via emulator option 6 (bubble_sort_data.txt output).
-check the bubble_sort_data.txt file.

EMULATOR (emulator.cpp)
- Loads binary object file into a 16MB memory array.
- Implements all 19 opcodes with correct semantics.
- Detects integer overflow (adc, add, sub), out-of-bounds memory access(ldl, stl, ldnl, stnl), and infinite loops (> 10^6 instructions). (logged in errorlog.txt)
- Menu options: ISA printout, trace, step, before, after, after+data.
- Option 6: writes code region, data region and final register state to <basename>_data.txt instead of printing to screen.

TEST CASES:All three assemble cleanly and produce correct filename_data.txt output.
- kadane.asm: Kadane's maximum sub-array. Input {-2,1,-3,4,-1,2}, result A=5.
- second_largest.asm: second largest element. Input {3,1,4,1,5,9}, result A=5.
- sumofarray.asm: Sum of array and its average values in data segment. Input {10,20,30,40,50},
  result:
  sum 000000C8  |  00000096  |  (150)   
  avg 000000C9  |  0000001E  |  (30)

One Fail Test Case: errors logged successfully 